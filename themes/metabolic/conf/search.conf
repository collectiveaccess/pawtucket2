#
# Defines searches to be executed on a "multisearch" – a search across several types of records at once
#
#
cache_timeout = 0


multisearchTypes = {
	actions = {
		displayName = _(Actions),
		labelSingular = _("action"),
 		labelPlural = _("actions"),
		table = ca_collections,
		restrictToTypes = [],
		view = Search/search_subview_list_html.php,
		itemsPerPage = 12,
		itemsPerColumn = 2,
		resultTemplate = ^ca_collections.preferred_labels.name,
		# --- sortControlType = dropdown or list
		sortControlType = list,
		sortBy = {
			Relevance = _natural,
			Identifier = ca_collections.idno,
			Name = ca_collection_labels.name
		},
		sortDirection = {
			Relevance = asc,
			Identifier = desc,
			Title = asc
		}
	},
	exhibitions = {
		displayName = _(Exhibitions),
		labelSingular = _("exhibition"),
 		labelPlural = _("exhibitions"),
		table = ca_occurrences,
		restrictToTypes = [exhibition],
		view = Search/search_subview_list_html.php,
		itemsPerPage = 12,
		itemsPerColumn = 2,
		resultTemplate = ^ca_occurrences.preferred_labels.name,
		# --- sortControlType = dropdown or list
		sortControlType = list,
		sortBy = {
			Relevance = _natural,
			Identifier = ca_occurrences.idno,
			Name = ca_occurrence_labels.name
		},
		sortDirection = {
			Relevance = asc,
			Identifier = desc,
			Title = asc
		}
	},
#	chronology = {
#		displayName = _(Events),
#		labelSingular = _("event"),
 #		labelPlural = _("events"),
#		table = ca_occurrences,
#		restrictToTypes = [action],
#		view = Search/search_subview_list_html.php,
#		itemsPerPage = 12,
#		itemsPerColumn = 2,
#		resultTemplate = ^ca_occurrences.preferred_labels.name,
		# --- sortControlType = dropdown or list
#		sortControlType = list,
#		sortBy = {
#			Relevance = _natural,
#			Identifier = ca_occurrences.idno,
#			Name = ca_occurrence_labels.name
#		},
#		sortDirection = {
#			Relevance = asc,
#			Identifier = desc,
#			Title = asc
#		}
#	},
	objects = {
		displayName = _(Assets),
		labelSingular = _("asset"),
 		labelPlural = _("assets"),
		table = ca_objects,
		restrictToTypes = [],
		#omitChildRecords = 1,
		#view = Search/ca_objects_search_subview_html.php,
		view = Search/ca_objects_search_subview_cascading_html.php,
		itemsPerPage = 24,
		#resultTemplate = "<div class='card-text mb-1'><l><small class='text-muted'>^ca_objects.idno</small></l></div><div class='card-title mb-3'><l>^ca_objects.preferred_labels.name</l></div><div class='card-text'>Description or longer text could go here.  Lorem ipsum dolor Lorem ipsum dolor Lorem ipsum dolor.</div>",
		resultTemplate = "<div class='card-title mb-2'><l><if rule='^ca_objects.type_id =~ /Album/'>Album: </if>^ca_objects.idno</l><ifdef code='ca_objects.altID'> (^ca_objects.altID)</ifdef><if rule='^ca_objects.type_id =~ /Album/'><br/><l>^ca_objects.preferred_labels.name</l></if></div>",
		# --- sortControlType = dropdown or list
		sortControlType = list,
		sortBy = {
			Relevance = _natural,
			Identifier = ca_objects.idno,
			Title = ca_object_labels.name
		},
		sortDirection = {
			Relevance = asc,
			Identifier = desc,
			Title = asc
		}
	}
}
advancedSearchTypes = {
	optics = {
		displayName = _(Optics Division),
		table = ca_objects,
		restrictToTypes = [photography],
		view = Search/ca_objects_advanced_search_optics_html.php, 
		itemsPerPage = 10,
		views = {
			images = {},
			list = {},
		},			
		sortBy = {
			Entity = ca_entity_labels.surname;ca_entity_labels.forename,
			Identifier = ca_objects.idno,
			Name = ca_object_labels.name,
		}
	},	
	objects = {
		displayName = _(Objects),
		table = ca_objects,
		restrictToTypes = [],
		view = Search/ca_objects_advanced_search_objects_html.php, 
		itemsPerPage = 10,
		views = {
			images = {},
			list = {},
		,			
		sortBy = {
			Entity = ca_entity_labels.surname;ca_entity_labels.forename,
			Identifier = ca_objects.idno,
			Name = ca_object_labels.name,
		}
	},
}
views = {
	images = {
		icon = glyphicon-th
	},
	list = {
		icon = glyphicon-list
	},
	timeline = {
		icon = glyphicon-time
	},
	map = {
		icon = glyphicon-globe
	}
}

#
# Other options
#

# default number of results per page/ajax load
defaultHitsPerBlock = 36

# add a wildcard "*" to the end of all searches? 
matchOnStem = 0