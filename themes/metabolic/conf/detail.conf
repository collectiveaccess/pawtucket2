

# Allow download of representation on object detail page?
# accepted values include: anyone, logged_in, logged_in_privileged
allow_ca_objects_representation_download = anyone


#
# Details supported in this theme
#
detailTypes = {
	objects = {
		displayName = _(Objects),
		table = ca_objects,
		restrictToTypes = [],
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			
			#
			# Sharing
			#
			enableComments = 1,
			enableShare = 0,
			enablePDF = 1,
			enableInquire = 0,
			enableDownloadAll = 1,
			downloadAllTypes = {
				"Original Media" = download_high,
				"Low Res Media" = download_low
			},
			
			#
			# Representation viewer
			# 
			representationViewerPrimaryOnly = 0,
			representationViewerDontShowPlaceholder = 0,
			representationViewerCaptionTemplate = <div class='small'>^ca_object_representations.preferred_labels.name</div>,
			
			#
			# Maps
			#
			mapAttributes = [ca_objects.georeference],
			mapWidth = 100%,
			mapHeight = 200,	
			mapZoomLevel = 12,			
			mapContentTemplate = ^ca_objects.idno,
			
			# Customize export download file names; can be set to a display template or static text
			pdfExportTitle = ^ca_objects.preferred_labels.name,
			
			#
			# Annotations
			#
			# display annotations? Valid values are: viewer (in viewer), div (in external div with class #detailAnnotations), none (no display) [Default is none]
			displayAnnotations = div,
			
			# template used to format annotation in list; evaluated relative to the annotation
			displayAnnotationTemplate = ^ca_representation_annotations.preferred_labels.name (^ca_representation_annotations.duration%asTimecode=hms),
			# --- enable mag glass option in media toolbar
			zoom = 1,
			
			# og tags for social media sharing
			og_title = ^ca_objects.idno,
			og_description = "^ca_objects.preferred_labels.name - © Metabolic Studio",
			og_type = website,
			#fb_app_id = 
		},
		# --- aliases to this detail page - these map to browses that return this type of detail, only need to be defined if the key of this array differs from the browses that find this record type
		#aliases = []
	},
	entities = {
		displayName = _(People),
		table = ca_entities,
		restrictToTypes = [],
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			enableComments = 0,
			enableShare = 0,
			map_attribute = ca_places.georeference,
			map_width = 350,
			map_height = 200,
			
			# og tags for social media sharing
			og_title = ^ca_entities.preferred_labels.name,
			og_description = ^ca_entities.description,
			og_type = website,
			#fb_app_id = 
		}
	},
	occurrences = {
		displayName = _(Exhibitions),
		table = ca_occurrences,
		restrictToTypes = [],
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			enableComments = 0,
			enableShare = 0,
			enableInquire = 0,
			
						
			# og tags for social media sharing
			og_title = ^ca_occurrences.preferred_labels.name,
			og_description = ^ca_occurrences.description,
			og_type = website,
			#fb_app_id = 
		}
	},
	places = {
		displayName = _(Places),
		table = ca_places,
		restrictToTypes = [],
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			enableComments = 0,
			enableShare = 0,
		}
	},
	collections = {
		displayName = _(Collections),
		table = ca_collections,
		restrictToTypes = [],
		disableRelatedItemsBrowse = true,
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			enableComments = 0,
			enableShare = 0,
			enablePDF = 0,
			enableInquire = 0,
			# --- exported pdf has label of record, leave blank to default to export_results
			pdfExportTitle = label,
			
			# og tags for social media sharing
			og_title = ^ca_collections.preferred_labels.name,
			og_description = ^ca_collections.description,
			og_type = website,			

		}
	},
	annotations = {
		displayName = _(Annotations),
		table = ca_representation_annotations,
		restrictToTypes = [],
		options = {
			nextLink = <span>Next</span><ion-icon name='ios-arrow-forward'></ion-icon>,
			previousLink = <ion-icon name='ios-arrow-back'></ion-icon><span>Prev</span>,
			resultsLink = <ion-icon name='ios-arrow-up'></ion-icon><span>Back</span>,
			enableComments = 0,
			enableShare = 0,
		}
	}
}