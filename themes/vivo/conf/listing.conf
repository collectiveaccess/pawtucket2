#
# Defines listings supported by Listing controller
#
#
listingTypes = {
	subject_guides = {
		displayName = _(Subject Guides),
		table = ca_occurrences,
		restrictToTypes = [subject_guide],
		view = Listing/ca_subject_guides_listing_subview_html.php,
		sortBy = {
			Name = ca_occurrence_labels.name
		},
		sortDirection = {
			Name = asc
		}
	}
}