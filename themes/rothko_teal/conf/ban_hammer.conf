enabled = 0
mode = absolute
threshold = 0.9

ip_whitelist = [
	66.44.127.*
]

plugins.UserAgent = {
    banned_useragents = [
         "MJ12bot", "AhrefsBot" , "GoogleBot", "SemrushBot", "opensiteexplorer", "DotBot", "UCBrowser","serpstatbot", "LieBaoFast", "Bytespider", "zh-CN", "splash", "Bing", "DotBot", "Yandex", 
         "BLEXBot", "PetalBot", "Barkrowler", "webmeup", "dotbot", "bot", "crawl","spider", "face", "anthr", "Amazon"
    ]
}



plugins.RequestFrequency = {
    # Maximum number of page loads per second allows before we ban
    frequency_threshold = 5,
    
    # Ban weight to return (1.0 = absolutely ban)
    ban_probability = 1.0,
}

plugins.IPAddress = {
    # List of ip addresses to ban
    # You may use wildcards to match ranges
    # Eg. 10.55.*.*
    banned_ip_addresses = [
        127.0.0.10
    ]
}
