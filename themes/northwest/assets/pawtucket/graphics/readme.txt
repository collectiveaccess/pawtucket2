#This directory is intentionally left blank, as all graphics will fall back to those in the "default" theme.  
#To override graphics, copy it from the default theme into this folder in your custom theme.