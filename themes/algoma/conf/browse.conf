# Browse cache TTL
cache_timeout = 0

# Highlight search terms in results?
do_highlighting = 0


# BrowseTypes keys to include in general search. 
# This also controls the order blocks of results appear in general search
generalSearchTargets = [all_collections, items]

# Available named browses
browseTypes = {
	collections = {
		displayName = _(Collections),
		labelSingular = _("collection"),
 		labelPlural = _("collections"),
		table = ca_collections,
		restrictToTypes = [collection],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		dontExpandTypesHierarchically = true,
		
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = { 
			list = {
				result_caption = "<div class='card-title text-center'>^ca_collections.preferred_labels</div>",
				list_format = text, # --- image or text where text is a simple grid of cards with no media
			}
		},
		
		sortBy = {
			Title = ca_collection_labels.name,
			Identifier = ca_collections.idno
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Title = asc,
			Identifier = asc,
		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels</l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		},
		baseCriteria = {
			collection_facet = [2033], #Algoma University archive repository
		}
	},
	files = {
		displayName = _(Files),
		labelSingular = _("file"),
 		labelPlural = _("files"),
		table = ca_collections,
		restrictToTypes = [file],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		dontExpandTypesHierarchically = true,
		
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = {
			images = {
				result_caption = "<div class='card-title'><small class='text-body-secondary'>^ca_collections.idno</small><div class='fw-medium lh-sm fs-5'><l>^ca_collections.preferred_labels</l></div></div><ifdef code='ca_collections.scope_content'><p class='card-text small lh-sm text-truncate'>^ca_collections.scope_content</p></ifdef>",
				image_format = contain, # --- contain or cover - default is cover
			}
		},
		
		sortBy = {
			Title = ca_collection_labels.name,
			Identifier = ca_collections.idno
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Title = asc,
			Identifier = asc,
		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels</l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		},
		baseCriteria = {
			collection_facet = [2033], #Algoma University archive repository
		}
	},
	# --- used for search
	all_collections = {
		displayName = _(Collections),
		labelSingular = _("collection record"),
 		labelPlural = _("collection records"),
		table = ca_collections,
		restrictToTypes = [],
		dontShowInBrowseMenu = 1, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		#dontExpandTypesHierarchically = true,
		
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = { 
			list = {
				result_caption = "<div class='card-title text-center'>^ca_collections.preferred_labels</div>",
				list_format = text, # --- image or text where text is a simple grid of cards with no media
			}
		},
		
		sortBy = {
			Title = ca_collection_labels.name,
			Identifier = ca_collections.idno
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Title = asc,
			Identifier = asc,
		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels</l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		},
		baseCriteria = {
			collection_facet = [2033], #Algoma University archive repository
		}
	},
	items = {
		displayName = _(Archival Items),
		labelSingular = _("archival item"),
		labelPlural = _("archival items"),
		table = ca_objects,
		restrictToTypes = [],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		
		#dontExpandTypesHierarchically = true,
		availableFacets = [],
		
		# formats available for display of results
		# possible values are: images, list, map
		views = {
			images = {
				result_caption = "<div class='card-title'><small class='text-body-secondary'>^ca_objects.idno</small><div class='fw-medium lh-sm fs-5'><l>^ca_objects.preferred_labels</l></div></div><ifdef code='ca_objects.work_description'><p class='card-text small lh-sm text-truncate'>^ca_objects.work_description</p></ifdef>",
				image_format = contain, # --- contain or cover - default is cover
			}
		},
		sortBy = {
			Identifier = ca_objects.idno,
			Title = ca_object_labels.name
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Identifier = asc,
			Title = desc
		},
		options = {
			selectMediaUsingRelationshipTypes = [relationshipType],
			selectMediaUsingTypes = [objectType],
			extendedInformationTemplate = ^ca_objects.description
		},
		generalSearchOptions = {
			resultCaption = "<div class='card-title'><small class='text-body-secondary'>^ca_objects.idno</small><div class='fw-medium lh-sm fs-5'><l>^ca_objects.preferred_labels</l></div></div><ifdef code='ca_objects.work_description'><p class='card-text small lh-sm text-truncate'>^ca_objects.work_description</p></ifdef>",
			searchView = general_image_results_html.php,
			imageFormat = cover, # --- contain or cover - default is cover
			numItemsToShow = 8,
			sortBy = _natural,
			sortDirection = asc,
		}
		
	},
}

views = {
	images = {
		icon = bi-grid-fill
	},
	list = {
		icon = bi-list-task
	},
	map = {
		icon = bi-globe
	}
}

# --- text for Browse menu item, default is "Browse" 
browse_menu_button_text = 

# --- default number of results per page/ajax load
defaultHitsPerBlock = 36

# --- set if you don't want the browse menu to be preloaded with facets for the first browse type on open
dontPreloadBrowseMenu = 1

# --------------------------------------------------------------------
# Configuration for collection browse
ca_collections = {
	name = _("Collections"),
	facets = {
		collection_facet = {
			type = hierarchy,
			group_mode = none,
			#single_value = 1950,
			
			label_singular = _("Hierarchy root"),
			label_plural = _("Hierarchy roots"),
			requires = x
		},
		century_facet = {
			type = normalizedDates,
			element_code = date_value,
			normalization = centuries,
			
			group_mode = alphabetical,
			
			label_singular = _("date"),
			label_plural = _("dates")\
		},
		decade_facet = {
			type = normalizedDates,
			element_code = date_value,
			normalization = decades,
			
			group_mode = alphabetical,
			
			label_singular = _("date"),
			label_plural = _("dates"),
			requires = century_facet
		},
		year_facet = {
			type = normalizedDates,
			element_code = date_value,
			normalization = years,
			
			group_mode = alphabetical,
			
			label_singular = _("date"),
			label_plural = _("dates"),
			requires = decade_facet
		},
		has_media_facet = {
			type = has,
			
			table = ca_object_representations,
			relationship_table = ca_collections_x_object_representations,
			restrict_to_types = [],
			restrict_to_relationship_types = [],
			
			label_yes = _("Has media"),
			label_no = _("Does not have media"),
			
			single_value = 1,
			
			label_singular = _("has media"),
			label_plural = _("has media"),
			description = _("Find files that have associated media.")
		},
		entity_creator_contributor_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relationship_table = ca_entities_x_collections,
			restrict_to_types = [],
			restrict_to_relationship_types = [creator,contributor],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("creator/contributor"),
			label_plural = _("creators/contributors")
		},
		entity_subject_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relationship_table = ca_entities_x_collections,
			restrict_to_types = [],
			restrict_to_relationship_types = [subject],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("subject"),
			label_plural = _("subjects")
		},
		place_facet = {
			type = authority,
			table = ca_places,
			relationship_table = ca_places_x_collections,
			restrict_to_types = [],
			
			group_mode = alphabetical,
			
			label_singular = _("place"),
			label_plural = _("places")
		}
	}
}