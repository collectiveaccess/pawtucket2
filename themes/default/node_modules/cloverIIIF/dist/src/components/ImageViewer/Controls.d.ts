/// <reference types="react" />
import { Options } from "openseadragon";
declare const Controls: ({ hasPlaceholder, options, }: {
    hasPlaceholder: boolean;
    options: Options;
}) => JSX.Element;
export default Controls;
