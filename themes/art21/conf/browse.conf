# Browse cache TTL
cache_timeout = 0

# BrowseTypes keys to include in general search. 
# This also controls the order blocks of results appear in general search
generalSearchTargets = [series, projects, shoots, artists, films, objects]

# Available named browses
browseTypes = {
	films = {
		displayName = _(Finished Films),
		labelSingular = _("film"),
 		labelPlural = _("films"),
		table = ca_objects,
		restrictToTypes = [video],
		dontShowInBrowseMenu = 1, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		
		#dontExpandTypesHierarchically = 1,
		availableFacets = [],
		
		# formats available for display of results
		# possible values are: images, list, map
		views = {
			images = {
				result_caption = "<div class='card-title'><small class='text-body-secondary'>^ca_objects.idno</small><div class='fw-medium lh-sm fs-5'><l>^ca_objects.preferred_labels</l></div></div>",
				image_format = cover, # --- contain or cover - default is cover
			}
		},
		sortBy = {
			Title = ca_object_labels.name,
			Date = ca_objects.common_date
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Title = asc,
			Date = desc
		},
#		options = {
#			selectMediaUsingRelationshipTypes = [relationshipType],
#			selectMediaUsingTypes = [objectType],
#			extendedInformationTemplate = ^ca_objects.description
#		},
		generalSearchOptions = {
			resultCaption = "<div class='card-title'><small class='text-body-secondary'>^ca_objects.idno</small><div class='fw-medium lh-sm fs-5'><l>^ca_objects.preferred_labels</l></div></div></ifdef>",
			searchView = general_image_results_html.php,
			imageFormat = cover, # --- contain or cover - default is cover
			numItemsToShow = 8,
			sortBy = _natural,
			sortDirection = asc,
		}
		
	},
	series = {
		displayName = _(Series),
		labelSingular = _("series"),
 		labelPlural = _("series"),
		table = ca_collections,
		restrictToTypes = [series],
		dontShowInBrowseMenu = 1, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = { 
			images = {
				result_caption = "<l>^ca_collections.preferred_labels<ifdef code='ca_collections.year'>, ^ca_collections.year</ifdef></l>",
			}, 
			list = {
				result_caption = "<l>^ca_collections.preferred_labels<ifdef code='ca_collections.year'>, ^ca_collections.year</ifdef></l>",
			}
		},
		
		sortBy = {
			Name = ca_collection_labels.name
		},
		sortDirection = {
			Name = asc
		}
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels<ifdef code='ca_collections.year'>, ^ca_collections.year</ifdef></l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		}
	},
	projects = {
		displayName = _(Projects),
		labelSingular = _("project"),
 		labelPlural = _("projects"),
		table = ca_collections,
		restrictToTypes = [project],
		dontShowInBrowseMenu = 1, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		dontExpandTypesHierarchically = true,
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = { 
			list = {
				result_caption = "<div class='card-title text-center'>^ca_collections.preferred_labels<ifdef code='ca_collections.year'>, ^ca_collections.year</ifdef></div>",
				list_format = text,
			}
		},
		
		sortBy = {
			Name = ca_collection_labels.name,
			Date = ca_collections.year
		},
		sortDirection = {
			Name = asc,
			Date = desc
		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels<ifdef code='ca_collections.year'>, ^ca_collections.year</ifdef></l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		}
	},
	artists = {
		displayName = _(Artists),
		labelSingular = _("artist"),
 		labelPlural = _("artists"),
		table = ca_entities,
		restrictToTypes = [],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		showLetterBarFrom = ca_entity_labels.surname,
		showLetterBarSorts = [ca_entity_labels.surname;ca_entity_labels.forename],
		
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = {
			list = {
				#result_caption = "<div class='card-title text-center'><ifdef code='ca_entities.idno'><small class='text-body-secondary'><l>^ca_entities.idno</l></small></ifdef><div class='fw-medium lh-sm fs-5'><l>^ca_entities.preferred_labels</l></div></div>",
				result_caption = "<div class='card-title text-center'><div class='fw-medium lh-sm fs-5'>^ca_entities.preferred_labels</div></div>",
				#image_format = cover, # --- contain or cover - default is cover
				list_format = text, # --- image or text where text is a simple grid of cards with no media

			}
		},
		
		sortBy = {
			Name = ca_entity_labels.surname;ca_entity_labels.forename
		},
		sortDirection = {
			Name = asc
		},
#		options = {
#			selectMediaUsingRelationshipTypes = [relationshipType],
#			selectMediaUsingTypes = [objectType]
#		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_entities.preferred_labels.displayname</l>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		},
		baseCriteria = {
			artist_facet = [1],
		}
		
	},
	shoots = {
		displayName = _(Shoots),
		labelSingular = _("shoot"),
 		labelPlural = _("shoots"),
		table = ca_collections,
		restrictToTypes = [shoot],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		dontExpandTypesHierarchically = true,
		# formats available for display of results
		# possible values are: images, list, timeline, map
		views = { 
			list = {
				result_caption = "<div class='card-title text-center'>^ca_collections.preferred_labels<ifdef code='ca_collections.work_date'><br/>^ca_collections.work_date</ifdef></div>",
				list_format = text,
			},
			images = {
				result_caption = "<l>^ca_collections.preferred_labels<ifdef code='ca_collections.work_date'><br/>^ca_collections.work_date</ifdef></l>",
			},
			images = {
				result_caption = "<div class='card-title'><div class='fw-medium lh-sm fs-5'>^ca_collections.preferred_labels<ifdef code='ca_collections.work_date'><br/>^ca_collections.work_date</ifdef></div></div></ifdef>",
				image_format = cover, # --- contain or cover - default is cover
			}, 
			map = {
				mapAttributes = [ca_places.georeference,ca_collections.georeference],
				width = 100%,
				height = 600px,
				minZoomLevel = 2,
				maxZoomLevel = 18,
				mapItemInfoTemplate =  "<l><div class='fw-bold'>^ca_collections.preferred_labels<ifdef code='ca_collections.work_date'>, ^ca_collections.work_date</ifdef><ifdef code='ca_places.preferred_labels.name'>, ^ca_places.preferred_labels.name</ifdef></div></l></ifdef>"
			}
		},
		
		sortBy = {
			Date = ca_collections.work_date,
			Name = ca_collection_labels.name
		},
		sortDirection = {
			Date = desc,
			Name = asc
		},
		generalSearchOptions = {
			resultCaption = "<div><l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'>^ca_collections.preferred_labels<ifdef code='ca_collections.work_date'>, ^ca_collections.work_date</ifdef></l></div>",
			searchView = general_list_results_html.php,
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		}
	},
	objects = {
		displayName = _("Films, Footage, Audio"),
		labelSingular = _("item"),
 		labelPlural = _("items"),
		table = ca_objects,
		restrictToTypes = [],
		dontShowInBrowseMenu = 0, # --- set to true or 1 to hide in main site navigation leave empty or 0 to display in menu
		
		#dontExpandTypesHierarchically = 1,
		availableFacets = [],
		
		# formats available for display of results
		# possible values are: images, list, map
		views = {
			list = {
				result_caption = "<div class='card-title'><span class='fs-6 text-body-secondary'>^ca_objects.type_id: ^ca_objects.idno</span><div class='fw-medium lh-sm fs-5'>^ca_objects.preferred_labels</div></div><ifdef code='ca_objects.num_clips|ca_objects.runtime|ca_objects.instantiationFileSize'><div class='card-text lh-sm'><ifdef code='ca_objects.num_clips'><ifdef code='ca_objects.num_clips'>^ca_objects.num_clips clips </ifdef><ifdef code='ca_objects.runtime'>^ca_objects.runtime </ifdef><ifdef code='ca_objects.instantiationFileSize'>^ca_objects.instantiationFileSize </ifdef></div></ifdef>",
				#image_format = contain, # --- contain or cover - default is cover
				list_format = text, # --- image or text where text is a simple grid of cards with no media
			},
			images = {
				result_caption = "<div class='card-title'><span class='fs-6 text-body-secondary'>^ca_objects.type_id: ^ca_objects.idno</span><div class='fw-medium lh-sm fs-5'>^ca_objects.preferred_labels</div></div><ifdef code='ca_objects.num_clips|ca_objects.runtime|ca_objects.instantiationFileSize'><div class='card-text lh-sm'><ifdef code='ca_objects.num_clips'><ifdef code='ca_objects.num_clips'>^ca_objects.num_clips clips </ifdef><ifdef code='ca_objects.runtime'>^ca_objects.runtime </ifdef><ifdef code='ca_objects.instantiationFileSize'>^ca_objects.instantiationFileSize </ifdef></div></ifdef>",
				image_format = cover, # --- contain or cover - default is cover
			}
		},
		sortBy = {
			Date = ca_objects.common_date,
			Title = ca_object_labels.name
		},
		# --- default sort direction for the sortBy options
		sortDirection = {
			Date = desc,
			Title = asc
		},
#		options = {
#			selectMediaUsingRelationshipTypes = [relationshipType],
#			selectMediaUsingTypes = [objectType],
#			extendedInformationTemplate = ^ca_objects.description
#		},
		generalSearchOptions = {
			resultCaption = "<l class='pt-3 pb-4 px-3 d-flex align-items-center justify-content-center bg-body-tertiary h-100 w-100 text-black'><div><div class='card-title'><span class='fs-6 text-body-secondary'>^ca_objects.type_id: ^ca_objects.idno</span><div class='fw-medium lh-sm fs-5'>^ca_objects.preferred_labels</div></div><ifdef code='ca_objects.num_clips|ca_objects.runtime|ca_objects.instantiationFileSize'><div class='card-text lh-sm'><ifdef code='ca_objects.num_clips'><ifdef code='ca_objects.num_clips'>^ca_objects.num_clips clips </ifdef><ifdef code='ca_objects.runtime'>^ca_objects.runtime </ifdef><ifdef code='ca_objects.instantiationFileSize'>^ca_objects.instantiationFileSize </ifdef></div></ifdef></div></l>",
			searchView = general_list_results_html.php,
			imageFormat = cover, # --- contain or cover - default is cover
			numItemsToShow = 7,
			sortBy = _natural,
			sortDirection = asc,
		}
		
	}
}

views = {
	images = {
		icon = bi-grid-fill
	},
	list = {
		icon = bi-list-task
	},
	map = {
		icon = bi-globe
	}
}

# --- text for Browse menu item, default is "Browse" 
browse_menu_button_text = 

# --- default number of results per page/ajax load
defaultHitsPerBlock = 36

# --- set if you don't want the browse menu to be preloaded with facets for the first browse type on open
dontPreloadBrowseMenu = 1


# Configuration for object browse
ca_objects = {
	name = _("Objects"),
	facets = {
		type_facet = {
			type = fieldList,
			field = type_id,
			
			group_mode = none,

			label_singular = _("Type"),
			label_plural = _("Types"),
			description = _("Find objects according to the Types defined in your collection profile."),
		},
		has_media_facet = {
			type = has,
			
			table = ca_object_representations,
			relationship_table = ca_objects_x_object_representations,
			restrict_to_types = [],
			restrict_to_relationship_types = [],
			
			label_yes = _("Has media"),
			label_no = _("Does not have media"),
			
			single_value = 1,
			
			label_singular = _("has media"),
			label_plural = _("has media"),
			description = _("Find objects that have associated media.")
		},	
		entity_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relationship_table = ca_objects_x_entities,
			restrict_to_types = [],
			
			individual_group_display = 1,
			
			groupings = {
				label = _("Name"), 
				relationship_types = _("Role"),
				type = _("Type")
			},
			group_mode = alphabetical,
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("person"),
			label_plural = _("people"),
			description = _("Find objects according to the people associated with your records.  You can browse by name&#44; role&#44; or entity type according to your configuration."),
		},	
		project_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_collections,
			relationship_table = ca_objects_x_collections,
			restrict_to_types = [project],
			
			individual_group_display = 1,
			
			group_mode = alphabetical,
			natural_sort = 0,
			order_by_label_fields = [name],
			
			indefinite_article = an,
			label_singular = _("project"),
			label_plural = _("projects"),
		},	
		series_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_collections,
			relationship_table = ca_objects_x_collections,
			restrict_to_types = [series],
			
			individual_group_display = 1,
			
			group_mode = alphabetical,
			natural_sort = 0,
			order_by_label_fields = [name],
			
			indefinite_article = an,
			label_singular = _("series"),
			label_plural = _("series"),
		}


	}
}
# --------------------------------------------------------------------
# Configuration for entity browse
ca_entities = {
	name = _("Entities"),
	facets = {
		artist_facet = {
			type = has,
			table = ca_objects,
			relationship_table = ca_objects_x_entities,
			restrict_to_types = [video],
			restrict_to_relationship_types = [artist,subject],
			
			label_yes = _("Is artist"),
			label_no = _("Is not artist"),
			
			single_value = 1,
			
			
			label_singular = _("Artist?"),
			label_plural = _("Artist?")
		},
		series_facet = {
			type = authority,
			table = ca_collections,
			relative_to = ca_objects,
			relationship_table = ca_objects_x_collections,
			restrict_to_types = [series],
			
			group_mode = alphabetical,
			
			label_singular = _("series"),
			label_plural = _("series")
		},
		year_facet = {
			type = normalizedDates,
			element_code = year,
			normalization = years,
			relative_to = ca_collections,
			
			group_mode = alphabetical,
			
			label_singular = _("project year"),
			label_plural = _("project years")
		}
	}
}
# --------------------------------------------------------------------
# Configuration for collection browse
ca_collections = {
	name = _("Collections"),
	facets = {
		artist_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relationship_table = ca_entities_x_collections,
			restrict_to_relationship_types = [artist],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("artist"),
			label_plural = _("artists")
		},
		year_facet = {
			type = normalizedDates,
			element_code = year,
			normalization = years,
			
			group_mode = alphabetical,
			
			label_singular = _("year"),
			label_plural = _("years")
		},
		shoot_year_facet = {
			type = normalizedDates,
			element_code = work_date,
			normalization = years,
			
			group_mode = alphabetical,
			
			label_singular = _("year"),
			label_plural = _("years")
		},
		place_facet = {
			type = authority,
			table = ca_places,
			relationship_table = ca_places_x_collections,
			restrict_to_types = [],
			
			group_mode = alphabetical, # --- chnage to hierarchical when data is more structured 
			order_by_label_fields = [name_sort],
			
			natural_sort = 0,
			# Set to non-zero value to display hierarchy on items in this facet
			show_hierarchy = 1,
			
			# Character(s) to place between elements of the hierarchy
			hierarchical_delimiter = &nbsp;⬅&nbsp;,
			
			# Number of items to trim off the top (leave blank or set to 0 to trim nothing)
			remove_first_items = 0,
			
			# Maximum length of hierarchy to display (leave blank to return hierarchy unabridged)
			hierarchy_limit = 3,
			
			# can be ASC or DESC (default is DESC)
			hierarchy_order  = ASC,
			
			label_singular = _("place"),
			label_plural = _("places")
		},
		series_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_collections,
			relationship_table = ca_collections_x_collections,
			restrict_to_types = [series],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [name],
			
			indefinite_article = an,
			label_singular = _("Series"),
			label_plural = _("Series")
		},
		crew_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relationship_table = ca_entities_x_collections,
			restrict_to_relationship_types = [crew],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("Crew"),
			label_plural = _("Crews")
		},
		has_media_facet = {
			type = has,
			
			table = ca_object_representations,
			relationship_table = ca_collections_x_object_representations,
			restrict_to_types = [],
			restrict_to_relationship_types = [],
			
			label_yes = _("Has media"),
			label_no = _("Does not have media"),
			
			single_value = 1,
			
			label_singular = _("has media"),
			label_plural = _("has media")
		},
# --- this doesn't work... the relative_to parent collection
		shoot_artist_facet = {
			# 'type' can equal authority, attribute, fieldList, normalizedDates
			type = authority,		
			table = ca_entities,
			relative_to = ca_collections.parent,
			relationship_table = ca_entities_x_collections,
			restrict_to_relationship_types = [artist],
			
			group_mode = alphabetical,
			
			natural_sort = 0,
			order_by_label_fields = [surname, forename, middlename],
			
			indefinite_article = an,
			label_singular = _("Artist"),
			label_plural = _("Artist")
		},
		
	}
}