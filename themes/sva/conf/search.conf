#
# Defines searches to be executed on a "multisearch" – a search across several types of records at once
#
#
multisearchTypes = {

#	objects = {
#		displayName = _(Objects),
#		table = ca_objects,
#		restrictToTypes = [],
#		view = Search/ca_objects_search_subview_cascading_html.php,
#		itemsPerPage = 10,
#		resultTemplate = ^ca_collections.preferred_labels.name,
		# --- sortControlType = dropdown or list
#		sortControlType = list,
		
#		resultTemplate = "<div class='breadcrumbs'><l>^ca_objects.preferred_labels.name</l></div>",
		
		
#		sortBy = {
#			Relevance = _natural,
#			Identifier = ca_objects.idno,
#			Title = ca_object_labels.name
#		},
#		sortDirection = {
#			Relevance = asc,
#			Identifier = desc,
#			Title = asc
#		}
#	},
#	collections = {
#		displayName = _(Collections),
#		table = ca_collections,
#		restrictToTypes = [],
#		view = Search/search_subview_list_html.php,
#		itemsPerPage = 10,
		
#		resultTemplate = ^ca_collections.preferred_labels.name,
		
		# --- sortControlType = dropdown or list
#		sortControlType = list,
#		sortBy = {
#			Relevance = _natural,
#			Identifier = ca_collections.idno,
#			Name = ca_collection_labels.name
#		},
#		sortDirection = {
#			Relevance = asc,
#			Identifier = desc,
#			Title = asc
#		}
#	},	
	occurrences = {
		displayName = _(Exhibitions),
		table = ca_occurrences,
		restrictToTypes = [exhibitions],
		view = Search/search_subview_list_html.php,
		
		resultTemplate = ^ca_occurrences.preferred_labels.name,
		
		# --- sortControlType = dropdown or list
		sortControlType = list,
		itemsPerPage = 12,
		
		itemsPerColumn = 3,
		
		sortBy = {
			Relevance = _natural,
			Identifier = ca_occurrences.idno,
			Title = ca_occurrence_labels.name
		},
		sortDirection = {
			Relevance = asc,
			Identifier = desc,
			Title = asc
		}
	}
	entities = {
		displayName = _(People),
		table = ca_entities,
		restrictToTypes = [],
		view = Search/search_subview_list_html.php,
		itemsPerPage = 12,
		itemsPerColumn = 3,
		
		resultTemplate = ^ca_entities.preferred_labels.displayname,
		
		# --- sortControlType = dropdown or list
		sortControlType = list,
		sortBy = {
			Relevance = _natural,
			Identifier = ca_entities.idno,
			Name = ca_entity_labels.surname;ca_entity_labels.forename
		},
		sortDirection = {
			Relevance = asc,
			Identifier = desc,
			Title = asc
		},
		options = {
			selectMediaUsingRelationshipTypes = []
		}
	},


}
advancedSearchTypes = {
	objects = {
		displayName = _("Objects"),
		labelSingular = _("Object"),
 		labelPlural = _("Objects"),
		table = ca_objects,
		restrictToTypes = [],
		view = Search/search_subview_list_html.php.php, 
		itemsPerPage = 10,
		
		sortBy = {
			Entity = ca_entity_labels.surname;ca_entity_labels.forename,
			Identifier = ca_objects.idno,
			Name = ca_object_labels.name
		}
	}
}
views = {
	images = {
		icon = glyphicon-th
	},
	list = {
		icon = glyphicon-list
	},
	timeline = {
		icon = glyphicon-time
	},
	map = {
		icon = glyphicon-globe
	}
}

#
# Other options
#

# default number of results per page/ajax load
defaultHitsPerBlock = 36

# add a wildcard "*" to the end of all searches? 
matchOnStem = 0
