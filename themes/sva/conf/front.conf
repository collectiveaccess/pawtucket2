# Code of set to render on front page
set_code = frontPage

# show multiple slides (images) or one at a time
set_show_multiple_slides = 1

# Template for caption text under each display object
front_page_set_item_caption_template = <span class='text-black'>^ca_objects.preferred_labels.name</span>

# Randomize order of set?
set_random = 1

# Restrict display of set items by object intrinsic values
# Set each intrinsic field code as a key, with the values to filter by in a list
intrinsic_filter = {}

# Code of set to render on front page
ca_objects_set_code = frontPageObjects
# Heading above 
ca_objects_heading = Objects
# Template for caption text under each display object
ca_objects_front_page_set_item_caption_template = <span class='text-black'>^ca_objects.preferred_labels.name</span>
# Randomize order of set?
ca_objects_set_random = 0

# Code of set to render on front page
ca_collections_set_code = frontPageCollections
# Heading above 
ca_collections_heading = Collections
# Template for caption text under each display object
ca_collections_front_page_set_item_caption_template = <span class='text-black'>^ca_collections.preferred_labels.name</span>
# Randomize order of set?
ca_collections_set_random = 0

# Code of set to render on front page
ca_occurrences_set_code = frontPageOccurrences
# Heading above 
ca_occurrences_heading = SVA Exhibitions and Events
# Template for caption text under each display object
ca_occurrences_front_page_set_item_caption_template = <span class='text-black'>^ca_occurrences.preferred_labels.name</span>
# Randomize order of set?
ca_occurrences_set_random = 0
