default_action = Front/Intro

#
# Set this if you want urls that refer to specified items to use the identifier 
# rather than CollectiveAccess internal ID. The former is more descriptive but will
# not work if you have duplicate identifiers in your catalogue!
#
use_identifiers_in_urls = 1

#
# Front page set code
#
front_page_set_code = frontPage
do_content_caching = 0

disable_lightbox = 1

locale_default = en_GB

ui_locales = [en_GB]

global_template_values = {
	suburb_history = {
		name = History of the Suburb,
		description = A Brief History of Hampstead Garden Suburb,
		width = 720px,
		height = 900px,
		usewysiwygeditor = 1
	},
        museum_help = {
                name = Using the Museum,
                description = Information on using the Digital Collections of the Hampstead Garden Suburb Virtual Museum,
                width = 720px,
                height = 250px,
                usewysiwygeditor = 1
        },
        contact_us = {
                name = Contact Details,
                description = Contact HGSH with any questions pertaining to this site, our work or the materials found in the Virtual museum,
                width = 720px,
                height = 250px,
                usewysiwygeditor = 1
        },
        donate_page_text = {
                name = Donate page,
                description = Donate page text,
                width = 720px,
                height = 250px,
                usewysiwygeditor = 1
        }
}

news_page_set_code = news_landing

