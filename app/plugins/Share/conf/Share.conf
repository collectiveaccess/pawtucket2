# Set to 0 to disable the Share plugin
enabled = 1

# require login
require_login = 0

# include media as attachment for object sharing?
enable_media_attachment = 1

# require message text
require_message_text = 0

# --- NOTE: media version to attach is set in media_display.conf in the email array